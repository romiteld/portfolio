import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Interactive AI Agents | AI Demos',
  description: 'Watch multiple AI agents collaborate on a task with a simple simulation.'
}

export default function InteractiveAgentsLayout({ children }: { children: React.ReactNode }) {
  return children
}
